# 无边记 AI - 产品功能综述

> 基于无限画布的智能笔记工具，结合 AI 扩展思维边界

## 产品定位

Web 端思维工具，灵感源自 Apple Freeform。通过无限画布和 AI 辅助，帮助用户组织和扩展思维。

**核心理念**：人脑工作记忆有限，需要外部工具记录和延展思考。AI 作为思维伙伴，协助深化和结构化想法。

---

## 1. 核心功能

### 1.1 无限画布

**画布操作**：
- 缩放：0.1x - 3x（Ctrl/Cmd + 滚轮）
- 平移：Shift + 拖动 / 中键拖动 / 滚轮
- 自动居中显示所有节点

**节点类型**：
- **文本卡片**：结构化信息、长文本记录（可调大小、自定义颜色、纸质纹理）
- **便签**：快速想法、临时笔记（淡黄色背景、200x200 固定尺寸）
- **思维导图** *[已禁用]*：层级关系、自动布局（代码保留）

**交互**：
- 编辑：双击节点，Ctrl/Cmd + Enter 保存，Esc 取消
- 选择：单击选中，Ctrl/Cmd + 点击多选
- 快捷键：Delete 删除、Ctrl/Cmd + Z 撤销、Tab 显示 AI 工具栏、Shift 显示属性面板
- 样式：9种文字颜色，7种背景色，字体大小/粗细可调

### 1.2 AI 智能功能

**节点级操作**（claude-3-5-haiku-20241022）：
- **扩写**：将简短想法扩展成详细内容（选中节点 + Tab + 扩写）
- **总结**：提炼长文本核心要点（选中节点 + Tab + 总结）

**AI 对话系统**（claude-sonnet-4-5-20250929）：
- **上下文感知**：AI 读取整个画布内容，自动检测用户修改
- **流式输出**：实时显示 AI 生成内容
- **工具调用**：AI 可创建文本框和便签（需用户确认）
- **多会话支持**：每个画布支持多个独立对话
- **分屏模式**：聊天窗口可固定在左侧（30%-50%宽度可调）

**AI 人格**：
- 深思熟虑的思考者，关注逻辑和本质
- 冷静克制，简洁精确，不使用多余符号
- 工程师的严谨 + 哲学家的洞察
- 只在有实质价值时创建节点

### 1.3 数据管理

**本地优先**（IndexedDB + Dexie.js）：
- 离线完全可用，零延迟响应
- 自动持久化，支持撤销/重做
- 无需服务器依赖

**可选云同步**（Supabase）：
- 全量同步（登录时）+ 增量同步（每30秒）
- 冲突解决：最新时间戳优先
- 离线队列：网络恢复后自动上传
- Row-Level Security 数据隔离

**认证**：
- 邮箱 + 密码登录（注册已禁用，需管理员手动添加用户）
- 未登录可使用完整本地功能
- 登录后支持多设备同步

---

## 2. 技术架构

### 2.1 技术栈

**前端**：
- Next.js 15.5.6 + React 19 + TypeScript 5
- Tailwind CSS 4（自然纸感设计系统）
- Zustand 5.0.8（状态管理）
- Dexie.js 4.2.1（IndexedDB）
- Supabase 2.76.1（云数据库 + 认证）

**后端**：
- Next.js API Routes（SSE 流式响应）
- Claude API 代理：`https://lumos.diandian.info/winky/claude`
- PostgreSQL（Supabase）

### 2.2 核心组件

**Canvas 层**：主画布、工具栏、画布切换器、设置菜单
**节点层**：TextNode、StickyNote、MindMapNode（保留）
**AI 层**：AIToolbar、ChatButton、ChatWindow、ChatResizer
**辅助层**：PropertyPanel、SyncStatus、AddToButton

### 2.3 性能优化

**已实现**：React.memo 记忆化、防抖同步（500ms）、IndexedDB 索引优化
**待优化**：视口裁剪、虚拟滚动、聊天历史分页

---

## 3. 设计系统

**自然纸感主题**（`app/globals.css`）：
- 主背景：`#F8F4EF`（温暖象牙白）
- 次背景：`#EDE4D5`（羊皮纸色）
- 主文字：`#3D342C`（深咖啡棕）
- 强调色：`#8B8E63`（橄榄薄雾绿）、`#B4723C`（焦糖陶土色）

**视觉效果**：
- `.glass-effect`：3D 玻璃边缘效果
- `.paper-texture` / `.paper-texture-light`：纸质纹理
- `.elastic-transition`：弹性缓动动画
- 统一圆角：`rounded-2xl`

---

## 4. 开发指南

**环境配置**：
```bash
# .env.local 必需
ANTHROPIC_API_KEY=your_api_key_here

# .env.local 可选（云同步）
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key
```

**常用命令**：
```bash
npm run dev      # 开发服务器 (localhost:3000)
npm run build    # 生产构建
npm run lint     # ESLint 检查
```

**路径别名**：`@/` 映射到项目根目录

---

## 5. 功能限制与路线图

**当前限制**：
- ❌ 思维导图创建（已禁用）
- ❌ 节点连接线（仅思维导图支持）
- ❌ 图片/文件上传
- ❌ 协作编辑
- ❌ 导出功能（PDF/Markdown）
- ⚠️ 节点数 >1000 可能卡顿

**计划功能**：
- 短期：节点连接线、Markdown 导出、命令面板、搜索
- 中期：多媒体支持、画布模板、更多 AI 模型
- 长期：实时协作、移动端、知识图谱、插件系统

---

## 6. 适用场景

**个人使用**：
- 知识管理（读书笔记、学习路径、概念地图）
- 项目规划（头脑风暴、任务拆解、会议纪要）
- 写作辅助（大纲构思、素材收集、草稿扩写）

**教育场景**：
- 教师：课程内容设计、知识点结构化
- 学生：笔记整理、考试复习、论文构思

---

## 7. 核心优势

✅ **本地优先架构** - 隐私保护、离线可用
✅ **深度 AI 集成** - AI 作为思维伙伴而非单纯工具
✅ **精致设计系统** - 自然纸感，专注内容
✅ **灵活数据管理** - 本地存储 + 可选云同步

---

**当前定位**：个人生产力工具
**未来愿景**：协作式知识管理平台
**核心价值**：让思考可视化，让 AI 成为思维伙伴

---

*文档版本*：v1.1 (精简版)
*更新日期*：2025-01-03
